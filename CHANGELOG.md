# Changelog

## 3.0.0 — 2026-09-18

Rebuilt on the LXRCore v3 native API as lxr-census (was lxr-scoreboard). Nothing of the earlier build remains.

* Counts by core job type, duty counts, privacy for names and ids, what the law count allows
* Page on the LXR UI Kit; `Allows(id)` export for robberies; locales EN / KA; offline tests
